import { SearchCountriesDirective } from '../directives/search-countries.directive';

describe('SearchCountriesDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchCountriesDirective();
    expect(directive).toBeTruthy();
  });
});
